name: Validate deployment

on:
  pull_request:
  push:
    branches: [master]

permissions:
  contents: read

jobs:
  compose:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate Compose wiring and shell syntax
        run: python3 -m unittest discover -s tests -v
