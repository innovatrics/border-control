REGISTRY=registry.dot.innovatrics.com/border-control/

CIGS_VERSION=0.3.3

# 0.4.0: zone-notification engine (zone.* topics with a per-topic VPP/MCT/CIGS source switch,
# person_moved floor-plan coordinates, identity.linked). Additive GraphQL schema; same license.
HUB_VERSION=0.4.0

FRONTEND_VERSION=0.5.0
FRONTEND_PORT=8095
